import fonts from './fonts.theme';

const { baseFontSize: base } = fonts;

export default {
  padding: {
    xs: `${base * 1}px`,
    sm: `${base * 2}px`,
    md: `${base * 3}px`,
    lg: `${base * 4}px`,
    xl: `${base * 5}px`,
  },
};
